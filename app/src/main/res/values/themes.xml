<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema per la modalità chiara -->
    <style name="Theme.MasanielloBettingApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Personalizza il tema qui -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Altre personalizzazioni se necessarie -->
    </style>

    <!-- Tema per la modalità scura -->
    <style name="Theme.MasanielloBettingApp.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Personalizza il tema qui -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Altre personalizzazioni se necessarie -->
    </style>

</resources>
