<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MasanielloBettingApp"
        tools:targetApi="31">

        <!-- MainActivity, che funge da Home -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MasanielloBettingApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dichiarazione di CalcolaPuntataActivity -->
        <activity android:name=".CalcolaPuntataActivity"
            android:exported="false" /><!-- Exported false se non devi lanciarla da altre app -->
        <!-- Dichiarazione di SimulazioniActivity -->
        <activity android:name=".SimulazioniActivity"
            android:exported="false" /><!-- Exported false se non devi lanciarla da altre app -->
        <activity android:name=".ReportisticaActivity"
            android:exported="false"/>
        <activity android:name=".ImpostazioniActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />




    </application>

</manifest>
